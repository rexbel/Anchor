{
  "name": "anchor-edge",
  "private": true,
  "description": "Cloudflare Worker for anchor.nextrex.health: serve from the Mac, hand over to a Cloudflare Container when the Mac is down.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.140.0"
  }
}
